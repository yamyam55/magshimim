<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omer.projectapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpScreen" />
        <activity android:name=".OptionsScreen" />
        <activity android:name=".ChangeUserSettingsScreen" />
        <activity android:name=".ChangeUserFontsSettingsScreen" />
        <activity android:name=".CreateNewFontScreen" />
        <activity android:name=".WrittenTextToComputerFontScreen"></activity>
    </application>

</manifest>